<h1>課金記録</h1>
<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Game</th>
      <th>Amount</th>
      <th>Image</th>
      <th>Memo</th>
    </tr>
  </thead>
  <tbody>
    <% @charges.each do |charge| %>
        <td><%= charge.date %></td>
        <td><%= charge.game %></td>
        <td><%= charge.amount %></td>
        <td><%= image_tag(charge.image) if charge.image.present? %></td>
        <td><%= charge.memo %></td>
      </tr>
    <% end %>
  </tbody>
</table>